console. log (null > 0);
console. log (null == 0);
console. log (null >= 0); 
console. log (undefined == 0); 
console. log (undefined > 0);
console. log (undefined < 0);

// "===" is used for differentiate between datatype of the data
console. log ("2" === 2);